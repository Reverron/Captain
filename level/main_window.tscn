[gd_scene load_steps=19 format=3 uid="uid://b7okfrd288s7i"]

[ext_resource type="PackedScene" uid="uid://dejkcym2erm1l" path="res://level/main.tscn" id="1_o043x"]
[ext_resource type="Script" uid="uid://u5q4bunpqynb" path="res://level/radar_controller.gd" id="2_bso6d"]
[ext_resource type="PackedScene" uid="uid://d03w3uebf0ig6" path="res://player/mini_map.tscn" id="3_bso6d"]
[ext_resource type="PackedScene" uid="uid://bv4abk1tuw7oi" path="res://util/fuel_bar.tscn" id="3_n56ur"]
[ext_resource type="Script" uid="uid://dica8ta0jb0mv" path="res://player/health_bar.gd" id="4_a8etk"]
[ext_resource type="PackedScene" uid="uid://bqysgv85fsncy" path="res://util/game_over_screen.tscn" id="5_qyphg"]
[ext_resource type="PackedScene" uid="uid://b3vc3ph36wid8" path="res://util/shop.tscn" id="6_rk545"]
[ext_resource type="PackedScene" uid="uid://b0xv8a2br0nfc" path="res://monitor.tscn" id="8_a8etk"]
[ext_resource type="PackedScene" uid="uid://cvmm2mco26814" path="res://util/retro_effect_rect.tscn" id="8_qyphg"]
[ext_resource type="Script" uid="uid://bibkhgios5jko" path="res://level/game_control.gd" id="9_gvqxy"]
[ext_resource type="PackedScene" uid="uid://bvcyx2gvb822c" path="res://player/side_screen.tscn" id="10_gvqxy"]
[ext_resource type="PackedScene" uid="uid://dhtrkax4kv0w3" path="res://util/select_marker.tscn" id="12_dol8q"]

[sub_resource type="Curve2D" id="Curve2D_o043x"]

[sub_resource type="Environment" id="Environment_qyphg"]
background_mode = 3
background_canvas_max_layer = -2
ambient_light_source = 2
ambient_light_color = Color(0.85979897, 0.85979897, 0.85979897, 1)
glow_enabled = true
glow_blend_mode = 1
fog_light_color = Color(0.17775077, 0.49950022, 1, 1)
fog_light_energy = 0.1
adjustment_enabled = true
adjustment_contrast = 1.05
adjustment_saturation = 1.1

[sub_resource type="CompressedTexture2D" id="CompressedTexture2D_biafw"]
load_path = "res://.godot/imported/energy.png-d31ae10deb2f267a25814b3eac80d643.ctex"

[sub_resource type="AtlasTexture" id="AtlasTexture_aoaw7"]
atlas = SubResource("CompressedTexture2D_biafw")
region = Rect2(0, 0, 48, 48)

[sub_resource type="Shader" id="Shader_gvqxy"]
code = "shader_type canvas_item;

uniform float shake: hint_range(0, 10) = 0.015;
uniform float noiseQuality: hint_range(0.1, 250, 0.1) = 250;
uniform float noiseIntensity: hint_range(0, 0.05, 0.001) = 0.001;
uniform float offsetIntensity: hint_range(0, 0.05, 0.0001) = 0.0045;
uniform float colorOffsetIntensity: hint_range(0.1, 1.5, 0.001) = 0.2;
uniform float pixelSize: hint_range(1, 1024) = 200.0;
uniform float grainIntensity: hint_range(0, 1) = 0.04; // Intensity of film grain

uniform sampler2D screen_texture : hint_screen_texture, repeat_disable, filter_nearest;
uniform sampler2D overlay_texture : hint_default_transparent;
uniform bool use_overlay = true; // Toggle for using overlay texture
uniform bool use_lens_distortion = true; // Toggle for using lens distortion

uniform vec4 overlay_color : source_color = vec4(1.0, 1.0, 1.0, 10.0); // Allow changing overlay color
uniform float lens_distortion_strength: hint_range(0, 0.1) = 0.05; // Strength of lens distortion effect

varying vec2 previous_pos; // Previous fragment position

float rand(vec2 co)
{
    return fract(sin(dot(co.xy, vec2(12.9898, 78.233))) * 43758.5453);
}

float verticalBar(float pos, float uvY, float offset)
{
    float edge0 = (pos - shake);
    float edge1 = (pos + shake);

    float x = smoothstep(edge0, pos, uvY) * offset;
    x -= smoothstep(pos, edge1, uvY) * offset;
    return x;
}

void fragment() {
    vec2 iResolution = 1.0 / SCREEN_PIXEL_SIZE;
    vec2 uv = FRAGCOORD.xy / iResolution.xy;

    // Calculate velocity
    vec2 velocity = uv - previous_pos;

    // Apply pixelation effect
    uv = floor(uv * pixelSize) / pixelSize;

    // Add vertical bars distortion
    for (float i = 0.0; i < 0.71; i += 0.1313)
    {
        float d = mod(TIME * i, 1.7);
        float o = sin(1.0 - tan(TIME * 0.24 * i));
        o *= offsetIntensity;
        uv.x += verticalBar(d, uv.y, o);
    }

    // Add noise distortion
    float uvY = uv.y * noiseQuality;
    uvY = float(int(uvY)) * (1.0 / noiseQuality);
    float noise = rand(vec2(TIME * 0.00001, uvY));
    uv.x += noise * noiseIntensity;

    // Add color offset distortion
    vec2 offsetR = vec2(0.006 * sin(TIME), 0.0) * colorOffsetIntensity;
    vec2 offsetG = vec2(0.0073 * cos(TIME * 0.97), 0.0) * colorOffsetIntensity;

    float r = texture(screen_texture, uv + offsetR).r;
    float g = texture(screen_texture, uv + offsetG).g;
    float b = texture(screen_texture, uv).b;

    vec4 tex = vec4(r, g, b, 1.0);

    // Overlay with an optional texture
    if (use_overlay) {
        vec4 overlayColor = texture(overlay_texture, uv) * overlay_color; // Apply overlay color
        tex = mix(tex, overlayColor, overlayColor.a);
    }

    // Add film grain
    float grain = rand(uv) * 2.0 - 1.0; // Range from -1 to 1
    tex.rgb += grainIntensity * grain;

    // Apply lens distortion
    if (use_lens_distortion) {
        vec2 distortion = uv * lens_distortion_strength * (uv - 1.0);
        vec4 distorted_tex = texture(screen_texture, uv + distortion);
        tex = mix(tex, distorted_tex, 0.5);
    }

    // Apply final color
    COLOR = tex;

    // Store current position for next frame
    previous_pos = FRAGCOORD.xy / iResolution.xy;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_dol8q"]
shader = SubResource("Shader_gvqxy")
shader_parameter/shake = 0.010000000475
shader_parameter/noiseQuality = 76.80000114441215
shader_parameter/noiseIntensity = 0.001
shader_parameter/offsetIntensity = 0.0029999999241
shader_parameter/colorOffsetIntensity = 0.20900000666762
shader_parameter/pixelSize = 500.0000237025
shader_parameter/grainIntensity = 0.04
shader_parameter/use_overlay = true
shader_parameter/use_lens_distortion = true
shader_parameter/overlay_color = Color(1, 1, 1, 10)
shader_parameter/lens_distortion_strength = 0.014000000665

[node name="MainWindow" type="Window"]
canvas_item_default_texture_filter = 0
canvas_cull_mask = 4293918722
oversampling_override = 1.0
title = "???"
initial_position = 2
size = Vector2i(900, 600)
always_on_top = true
maximize_disabled = true
theme_override_colors/title_color = Color(0, 0, 0, 1)
theme_override_colors/title_outline_modulate = Color(0, 0, 0, 1)
theme_override_constants/title_outline_size = 50

[node name="Main" parent="." instance=ExtResource("1_o043x")]
unique_name_in_owner = true
visibility_layer = 3

[node name="MiniMapCamera" type="Camera2D" parent="."]
unique_name_in_owner = true
light_mask = 0
visibility_layer = 2
z_index = 10
position = Vector2(460, 40)
ignore_rotation = false
zoom = Vector2(0.12, 0.12)
limit_enabled = false
script = ExtResource("2_bso6d")

[node name="Path2D" type="Path2D" parent="MiniMapCamera"]
top_level = true
light_mask = 0
visibility_layer = 2
z_index = 35
position = Vector2(-460, -40)
curve = SubResource("Curve2D_o043x")

[node name="PathFollow2D" type="PathFollow2D" parent="MiniMapCamera/Path2D"]

[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
environment = SubResource("Environment_qyphg")

[node name="MainUI" type="CanvasLayer" parent="."]
layer = 2

[node name="HealthBar" type="TextureRect" parent="MainUI"]
unique_name_in_owner = true
visible = false
light_mask = 0
visibility_layer = 2
anchors_preset = 3
anchor_left = 1.0
anchor_top = 1.0
anchor_right = 1.0
anchor_bottom = 1.0
offset_left = -300.0
offset_top = -200.0
grow_horizontal = 0
grow_vertical = 0
pivot_offset = Vector2(30, 30)
texture = SubResource("AtlasTexture_aoaw7")
expand_mode = 1
script = ExtResource("4_a8etk")

[node name="GameOverScreen" parent="MainUI" instance=ExtResource("5_qyphg")]
unique_name_in_owner = true
visible = false
top_level = true
light_mask = 0
visibility_layer = 2

[node name="Shop" parent="MainUI" instance=ExtResource("6_rk545")]
unique_name_in_owner = true
visible = false
light_mask = 0
visibility_layer = 2

[node name="MiniMap" parent="." node_paths=PackedStringArray("player", "mini_map_camera") instance=ExtResource("3_bso6d")]
unique_name_in_owner = true
player = NodePath("../Main/PlayerCaptain")
mini_map_camera = NodePath("../MiniMapCamera")

[node name="RetroEffectRect" parent="MiniMap" instance=ExtResource("8_qyphg")]
material = SubResource("ShaderMaterial_dol8q")

[node name="GameControl" type="CanvasLayer" parent="."]
unique_name_in_owner = true
layer = -1
script = ExtResource("9_gvqxy")

[node name="SideScreen" parent="GameControl" instance=ExtResource("10_gvqxy")]
unique_name_in_owner = true

[node name="SelectMarker" parent="GameControl" instance=ExtResource("12_dol8q")]
unique_name_in_owner = true

[node name="FuelBar" parent="GameControl" instance=ExtResource("3_n56ur")]
unique_name_in_owner = true
light_mask = 0
visibility_layer = 2
custom_minimum_size = Vector2(345, 30)
offset_left = -170.0
offset_top = -121.0
offset_right = 175.0
offset_bottom = -91.0
theme_override_constants/outline_size = 3

[node name="Monitor" parent="." instance=ExtResource("8_a8etk")]

[editable path="GameControl/SideScreen"]
